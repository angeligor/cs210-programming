/*
CS210 Project Three - Corner Grocer
File: Menu.h
Purpose: Declare the ResumeMenu() which owns all console interactions.
Notes: Keeps UI concerns separate from data logic (ItemTracker).
*/

#ifndef MENU_H
#define MENU_H
// Drives the program: shows options, processes input, and calls ItemTracker.
void RunMenu();

#endif

